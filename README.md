# 재정경제부 언론 모니터링

재정경제부(가상) 언론 담당자를 위한 **로컬 전용 단일 사용자** 뉴스 모니터링 도구입니다.
매일 여러 차례 자동으로 네이버 뉴스를 검색해 관련 기사를 모으고, 기사를 소제목별로
자동 분류해 브라우저 화면에서 한눈에 훑어볼 수 있게 해줍니다. 별도의 외부 AI(LLM) API는
쓰지 않고, 분류·요약·키워드 추출을 모두 규칙 기반(단어 빈도 계산)으로 처리해 API 비용이
들지 않습니다.

## 목차

- [배경](#배경)
- [핵심 기능](#핵심-기능)
- [화면 구성](#화면-구성)
- [아키텍처](#아키텍처)
- [폴더 구조](#폴더-구조)
- [설치 및 실행](#설치-및-실행)
- [설정 방법](#설정-방법)
- [데이터 저장](#데이터-저장)
- [테스트](#테스트)
- [알려진 한계](#알려진-한계)
- [기술 스택](#기술-스택)
- [개발 방법론](#개발-방법론)

## 배경

기관 언론 모니터링 담당자는 보통 하루 몇 차례씩 포털에서 관련 키워드를 직접 검색해,
그중 자기 부처와 관련된 기사만 골라 정리한 뒤 보고용 텍스트로 만드는 작업을 반복합니다.
이 앱은 이 과정 중 "검색 → 수집 → 분류 → 정리 → 내보내기"를 자동화하고, 사람은 마지막에
결과를 훑어보며 필요 없는 기사를 걸러내거나(숨기기) 순서를 다듬는 "큐레이션"만 하면
되도록 만들어졌습니다.

## 핵심 기능

### 자동 수집
- 설정한 시각(기본값 09:00 / 10:30 / 13:30 / 16:30)에 자동으로 네이버 뉴스 검색 API를
  호출해 등록된 키워드로 기사를 수집합니다. 시각과 하루 실행 횟수는 설정 화면에서
  최소 1개~최대 10개까지 자유롭게 편집할 수 있습니다(코드 수정 불필요).
- 앱이 꺼져 있어 놓친 회차가 있으면, 다음에 앱을 켰을 때 가장 최근 놓친 회차 1개만
  즉시 보충 실행합니다(더 이전 회차는 건너뜀).
- 검색은 키워드 2~7개까지 등록 가능하며, 2개 이상이면 OR(하나라도 포함)/AND(모두 포함)
  방식을 선택할 수 있습니다. 검색은 기사 원문(본문) 전체를 대상으로 합니다.
- 게시일이 당일(한국 시간 기준)인 기사만 수집하고, 지난 날짜 기사는 제외합니다.
- 네트워크 오류 등으로 실패하면 5분 간격으로 최대 3회 자동 재시도합니다.

### 자동 소제목 분류 (규칙 기반, LLM 없음)
- 기사 요약(네이버 API의 description)에 자주 등장하는 단어를 묶어, 매 회차마다 소제목을
  최대 5개까지 새로 생성합니다. 제목이 아니라 요약을 기준으로 삼는 이유는, 언론사마다
  제목 표현이 제각각이라 제목만으로는 같은 사건을 다룬 기사끼리도 겹치는 단어가 잘
  안 잡히기 때문입니다 — 보도자료를 비슷한 문장으로 옮겨 쓰는 요약문에서는 공통 단어가
  더 잘 드러납니다.
- 담당자가 완전히 확인할 것을 전제로 하므로 완벽한 분류일 필요는 없습니다.
- 소제목별 대표 요약(3문장·150자 이내)과 전체 주요 키워드(쉼표로 나열)를 화면 맨 아래에
  함께 보여줍니다.

### 언론사 우선순위
- 기본값: 방송사(KBS, MBC) → 주요 언론사(조선일보, 중앙일보, 한국경제 등) → 그 외 순.
- 설정 화면에서 원하는 언론사만 체크박스로 골라 화이트리스트로 지정하고, "↑"/"↓" 버튼으로
  순서를 직접 정할 수도 있습니다. 아무것도 안 고르면 기본 우선순위를 그대로 씁니다.

### 사진기사 자동 제외
- 제목에 사진기사 힌트 단어(포토, [포토], 사진, 화보, 포토뉴스, PHOTO — 대소문자 무시)가
  하나라도 있으면 수집 대상에서 아예 제외합니다.
- 단, `[속보]`가 붙어 있으면 다른 힌트 단어가 같이 있어도 예외로 항상 수집합니다(단신이어도
  속보는 놓치지 않기 위함).
- 완전히 동일한 제목의 기사는 언론사 우선순위가 가장 높은 것 1건만 남깁니다(중복 게재는
  허용하되, 진짜 중복은 걸러냄).

### 기사 큐레이션 (화면에서 직접 조작)
- **🗑️ 숨기기 / 되돌리기**: 보고에서 빼고 싶은 기사를 화면에서 바로 숨길 수 있습니다.
  원본 수집 데이터는 지우지 않고 "숨긴 기사 URL 목록"만 별도로 기록하며, 설정 화면
  "숨긴 기사 관리"에서 언제든 되돌릴 수 있습니다.
- **↑ / ↓ 순서 조정**: 같은 소제목 안에서 순서를 바꿀 수 있고, 소제목 맨 위/아래에서 한
  번 더 누르면 옆 소제목으로 기사를 통째로 옮길 수도 있습니다(소제목 경계 넘나들기).
- **✏️ 소제목 이름 바꾸기**: 자동 생성된 소제목 이름이 마음에 안 들면 직접 다른 이름으로
  바꿀 수 있습니다. 분류 로직 자체는 그대로 두고 화면에 보여주는 이름만 바뀝니다.

### 출력 형식 커스터마이징
- 기사 한 줄의 형식(기본값 `ㅇ (언론사) 기사제목`)을 `{outlet}`/`{title}` 자리표시자
  템플릿으로 자유롭게 바꿀 수 있습니다(예: `[언론사] 제목`). URL 줄과 게시일 미표시는
  고정입니다.

### 진입 화면
- 로고와 함께, 당일 주요 키워드를 무궁화를 연상시키는 방사형 워드클라우드로 보여줍니다.
  1위 단어는 가운데 가장 크고 굵은 금색, 2~4위는 안쪽 고리에 빨강, 5위 이하는 바깥
  고리에 분홍 계열로 배치됩니다. 통계적 정확도보다는 한눈에 훑어보는 용도입니다.

### 지난 기사 조회
- 최근 7일간 저장된 회차를 날짜별 → 시간대별 토글로 펼쳐볼 수 있습니다. 7일이 지난
  회차는 자동으로 삭제되어 조회 목록에서도 사라집니다.

### 내보내기 / 새로고침
- 소제목별 결과를 클립보드로 복사하거나 `.txt` 파일로 내보낼 수 있습니다. 버튼·아이콘
  글자는 복사 결과에 포함되지 않습니다.
- 메인 화면은 1분마다 자동으로 새로고침되어, 새 회차가 수집되면 열어둔 탭에도 자동
  반영됩니다. 화면 상단에 마지막 갱신 시각이 표시됩니다.

## 화면 구성

| 화면 | 파일 | 설명 |
|------|------|------|
| 진입 화면 | `home.html` | 앱 실행 시 처음 열리는 화면. 로고 + 당일 키워드 워드클라우드 |
| 메인 화면 | `index.html` | 최신 회차의 소제목별 기사 목록. 숨기기/순서 조정/이름 바꾸기 가능 |
| 지난 기사 | `history.html` | 최근 7일 회차를 날짜 → 시간대별로 조회 |
| 설정 | `http://127.0.0.1:8765/` | 아래 6개 하위 페이지로 구성된 설정 메뉴 |

설정 메뉴 하위 페이지 (세 그룹으로 구성):

1. **수집 관련**: 🔎 검색 키워드 · 📰 주요 언론사 선택 및 순서 지정 · ⏰ 스크랩 시간 및 횟수
2. **표시 관련**: 💡 형광펜 단어 · 🖊️ 본문 형식
3. **관리**: 🗑️ 숨긴 기사 관리

## 아키텍처

서버 프레임워크(FastAPI, Django 등) 없이, 파이썬 스크립트가 결과를 정적 HTML 파일
(`index.html`, `history.html`, `home.html`)로 생성하고 브라우저로 엽니다. 데이터는
SQLite 대신 로컬 JSON 파일(`data/`)에 저장합니다.

예외적으로 딱 한 곳, 설정 저장·기사 큐레이션(숨기기/순서 조정/이름 바꾸기)을 처리하기
위한 최소 로컬 HTTP 서버(`app/settings_server.py`, 표준 라이브러리 `http.server`만 사용,
`127.0.0.1`에서만 동작)를 둡니다. 이 서버는 위 세 정적 화면 파일도 같은 출처(origin)로
함께 서빙하는데, 이는 최신 브라우저가 보안상 http 페이지에서 file:// 경로로의 이동을
막기 때문에 필요한 우회입니다.

자바스크립트는 "꼭 필요한 곳만" 최소로 사용합니다 — 클립보드 복사, 기사 숨기기/순서
조정(`fetch`로 로컬 서버에 비동기 저장), 소제목 이름 바꾸기(`prompt()` + `fetch`) 네
곳뿐입니다. 나머지는 순수 HTML/CSS(`<details>`/`<summary>` 토글, `<meta http-equiv=
"refresh">` 자동 새로고침 등)로 구현합니다.

자세한 설계는 [DESIGN.md](DESIGN.md), 전체 요구사항은 [PRD.md](PRD.md), 작업 진행
이력은 [PLAN.md](PLAN.md)를 참고하세요.

## 폴더 구조

```
app/
  naver_api.py        # 네이버 뉴스 검색 API 호출, 언론사 도메인 판별
  filters.py           # 사진기사 제외, 완전 동일 제목 중복 제거, 언론사 화이트리스트
  classifier.py        # 요약 기반 소제목 자동 분류 (규칙 기반)
  summarizer.py        # 소제목별 대표 요약, 전체 주요 키워드 추출
  highlight.py          # 형광펜 단어 하이라이트
  tokenizer.py          # 한국어 간이 토큰화(조사 제거, 불용어, 날짜 패턴 제외)
  storage.py            # 회차별 기사 JSON 저장/조회/보관기간 관리
  curation.py           # 기사 숨기기/되돌리기, 순서 조정, 소제목 이름 바꾸기
  settings.py           # 설정값 저장/검증 (키워드, 언론사, 형광펜, 출력 형식, 스크랩 시간)
  scheduler.py          # 자동 실행 스케줄 루프, 놓친 회차 보충 실행
  scraper.py            # 검색→정렬→필터→저장 파이프라인
  renderer.py           # 메인 화면(index.html) 렌더링
  history_renderer.py   # 지난 기사(history.html) 렌더링
  landing_renderer.py   # 진입 화면(home.html), 워드클라우드 렌더링
  settings_server.py    # 설정 저장·큐레이션 API를 처리하는 로컬 HTTP 서버
  atomic_write.py       # 여러 스레드가 동시에 같은 파일에 써도 안전한 원자적 쓰기
main.py                 # 앱 진입점 (스케줄러·설정 서버 기동, 브라우저 오픈)
data/                   # 회차별 기사, 설정값 등 로컬 저장 데이터 (git에는 안 올라감)
tests/
  test_integration.py   # 수집→분류→화면→내보내기 전체 흐름 통합 테스트
PRD.md / PLAN.md / DESIGN.md  # 요구사항 / 작업 이력 / 설계 문서 (계속 갱신됨)
```

## 설치 및 실행

```bash
git clone https://github.com/sweetgreetings/news-scrap.git
cd news-scrap
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

`.env` 파일을 프로젝트 루트에 만들고 네이버 뉴스 검색 API 키를 넣습니다
([네이버 개발자센터](https://developers.naver.com)에서 애플리케이션 등록 후 발급,
"검색" API 사용 설정 필요):

```
NAVER_CLIENT_ID=발급받은_클라이언트_ID
NAVER_CLIENT_SECRET=발급받은_클라이언트_시크릿
```

실행:

```bash
python3 main.py
```

브라우저가 자동으로 열리며 진입 화면(`home.html`)을 보여줍니다. 앱을 켜둔 동안 설정한
시각마다 자동으로 수집이 실행됩니다. 종료하려면 터미널에서 `Ctrl+C`를 누르세요.

## 설정 방법

앱 실행 중 `http://127.0.0.1:8765/` 로 접속하면 설정 메뉴가 뜹니다. 각 하위 페이지에서
값을 바꾸고 저장하면 별도 코드 수정 없이 바로 다음 회차부터(또는 스크랩 시간처럼 다음
확인 주기 안에) 반영됩니다.

## 데이터 저장

`data/` 폴더 아래에 다음 파일들이 생성됩니다 (모두 git에서 제외됨):

- `articles/YYYY-MM-DD_HH-MM.json`: 회차별 수집 기사 원본 (언론사·제목·URL·요약)
- `settings.json`: 검색 키워드, 언론사 화이트리스트, 형광펜 단어, 출력 형식, 스크랩 시간
- `hidden_articles.json`: 숨긴 기사 URL 목록
- `group_overrides.json`: 소제목 경계를 넘어 수동으로 재배정한 기사 기록
- `group_labels.json`: 소제목에 붙인 사용자 지정 이름표

회차 데이터는 저장된 지 7일이 지나면 자동으로 삭제됩니다.

## 테스트

```bash
source .venv/bin/activate
python3 tests/test_integration.py
```

검색/정렬/필터, 화면 렌더링, 복사/내보내기 텍스트, 설정 연동, 빈 회차 처리, 보관 기간
정리까지 전체 파이프라인을 실제 데이터 폴더와 분리된 임시 폴더에서 검증합니다.

## 알려진 한계

- **인터넷 배포 불가**: 이 앱은 처음부터 "내 컴퓨터에서만 실행"을 전제로 설계됐습니다.
  - 스케줄러가 앱이 켜진 동안 계속 도는 무한 루프 구조라 서버리스(Vercel 등) 환경에서
    실행할 수 없습니다.
  - 기사·설정 데이터를 로컬 파일에 저장하므로, 서버리스 환경에서는 배포마다 초기화됩니다.
  - 설정 저장 서버에 **로그인/인증이 없습니다** — "로컬에서만 접속 가능"을 전제로
    의도적으로 만들지 않았습니다. 이 상태로 인터넷에 노출하면 누구나 로그인 없이
    키워드 변경, 기사 삭제 등이 가능해집니다.
  - 그러므로 이 저장소를 그대로 Vercel 등에 배포하지 마세요. 인터넷 공개가 꼭 필요하면
    별도의 아키텍처 재설계(실제 데이터베이스, 서버리스 스케줄, 인증 추가)가 필요합니다.
- 진입 화면 워드클라우드는 "당일"이 아니라 "가장 최근 저장된 회차"를 그대로 씁니다 —
  오늘 첫 스크랩 전에는 어제 마지막 회차의 키워드가 보일 수 있습니다.
- 컴퓨터가 잠자기 상태였던 동안에는 예약된 정시 실행이 되지 않으며, 깨어난 직후 가장
  최근에 놓친 회차 1개만 보충 실행합니다.
- 소제목 분류는 완벽한 정확도를 보장하지 않습니다(담당자가 검토·큐레이션하는 것을
  전제로 한 설계입니다).

## 기술 스택

- Python 3.9+
- `requests`, `python-dotenv` (외부 의존성은 이 두 개뿐)
- 외부 웹 프레임워크 없음 — 표준 라이브러리 `http.server`만 사용
- 순수 CSS 기반 UI, 자바스크립트는 위 아키텍처 절에 명시한 네 곳에만 최소 사용
- 대상 OS: Windows(단일 로컬 사용자, 인증 없음 전제)

## 개발 방법론

이 프로젝트는 PDCA(Plan → Design → Do → Check → Act) 방식으로, `PRD.md`(요구사항)
→ `PLAN.md`(작업 순서) → `DESIGN.md`(설계)를 먼저 정하고 하나씩 구현하며 세 문서를
계속 갱신하는 흐름으로 만들어졌습니다. 각 기능은 구현 후 실제 실행/브라우저 확인과
자체 코드 리뷰(gap-detector 등)를 거쳤습니다.
