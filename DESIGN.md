# DESIGN.md

> PLAN.md의 작업 순서를 실제로 어떻게 만들지 정하는 설계 문서입니다.

## 0. 진입 화면 [추가: 2026-07-23, 대폭 수정: 2026-07-24] (home.html — 앱이 브라우저로 맨 처음 여는 화면)

```
┌─────────────────────────────────────────────┐
│                  [로고 이미지]                   │  ← logo.svg 있으면 표시, 없으면 자리만 비움
│                                                │
│         재정경제부 온라인 뉴스 모아보기 🧺👀           │
│                                                │
│              tf                               │  ← 무궁화 연상 방사형 워드클라우드
│      민생물   경제부총리  밝혔습니다                  │     (아래 설명 참고)
│  기울이겠다고        관계장관    특별관리              │
│      7월에      2%대      3%                    │
│                                                │
│              👓 스크랩 보러 가기                    │  ← index.html로 이동
│              ⚙️ 설정                            │  ← 설정 메뉴로 이동
│                                                │
│           문의 📧 sweetgreetings@naver.com       │
└─────────────────────────────────────────────┘
```

- [수정: 2026-07-24] "당일 주요 키워드"라는 설명 문구는 뺐다 — 워드클라우드 자체로 직관적으로
  알 수 있어 불필요하다는 피드백.
- [수정: 2026-07-24] 워드클라우드 단어 추출 기준을 기사 제목 → 네이버 요약(description)으로
  바꿨다 (2번 항목과 같은 이유: 언론사마다 표현이 제각각인 제목보다 요약문에 공통 단어가 더
  잘 잡힌다).
- [수정: 2026-07-24] 워드클라우드를 무궁화를 연상시키는 방사형 3단 배치로 다시 구현했다 —
  실제 꽃 그림은 그리지 않고 단어 배치·색상만으로 표현한다(요청: "배경 그림 없이 단어
  배치만으로 무궁화 느낌"):
  - 1위 단어: 가운데, 가장 크게(2.1rem), 굵게(bold), 금색(`#B8860B`)
  - 2~4위: 안쪽 고리(반지름 19%), 빨강(`#C81E4C`)
  - 5위 이하: 바깥 고리(반지름 33%), 분홍 계열 3색 순환(`#D6499C`/`#C43D91`/`#B8348A`)
  - 각 단어 위치는 `math.radians`로 계산한 각도(고리 안에서 등간격)와 반지름(%)으로 정해지며,
    글자 크기는 언급 빈도에 비례해 고리별 최소~최대 사이로 보간한다.
  - 순수 노랑(`#F5D90A` 등)은 밝은 배경(`#F8FAFC`)에서 거의 안 보여서 쓰지 않는다 — 색 정체성은
    유지하되 어두운 금색(다크골드)으로 대체했다.
  - 컨테이너는 정사각형이 아니라 가로로 긴 사각형(300×220px)이다 — "글자 사이 여백이 너무
    많다"는 피드백으로 고리 반지름을 좁혀 중앙 단어를 중심으로 촘촘하게 모았다.
- 오늘 아직 수집된 회차가 없으면 "아직 수집된 키워드가 없습니다" 안내만 표시한다(에러 아님).
  다만 [알려진 한계] 이 워드클라우드는 실제로는 "오늘 날짜"가 아니라 "가장 최근에 저장된
  회차"를 그대로 쓴다 — 오늘 첫 스크랩 전에는 어제 마지막 회차의 키워드가 그대로 보인다.
- [수정: 2026-07-24] 로고 파일명이 `logo.png` → `logo.svg`로 바뀌었다 (사용자가 SVG 로고를
  제공). `BASE_DIR/logo.svg`가 있으면 보여주고, 없으면 그 자리를 건너뛴다. 로고에 있던 회색
  원형 테두리(`box-shadow`)는 제거했다(사용자 요청).

## 1. 화면 구성 (무엇이 어디에 보이는지)

```
┌─────────────────────────────────────────────┐
│ 언론 모니터링 09:00 기준                          │  ← 헤더
│ 🔄 13:53 업데이트                                │  ← [추가: 2026-07-24] 마지막 갱신 시각 표시기
│ [복사][내보내기][설정][📅 지난 기사 더보기]           │  ← [수정: 2026-07-24] 지난 기사 링크가
├─────────────────────────────────────────────┤     하단에서 여기로 이동
│ <소제목1>  ✏️                                  │  ← [추가: 2026-07-24] 소제목 이름 바꾸기 버튼
│  ㅇ (KBS) 기사제목 1          → 클릭 시 요약 표시     │
│    URL                    [↑][↓][🗑️]         │  ← [추가: 2026-07-24] 순서 조정·숨기기 버튼
│  ㅇ (조선일보) 기사제목 2                          │
│ ...                                           │
│ ※ "ㅇ (언론사) 기사제목" 줄의 형식은 설정 화면            │
│   "🖊️ 본문 형식"에서 {{outlet}}/{{title}} 자리표시자   │
│   템플릿으로 바꿀 수 있다 (예: "[언론사] 제목"). URL 줄과   │
│   게시일자 미표시는 이번 범위에서 그대로 유지한다.          │
├─────────────────────────────────────────────┤
│ <소제목2> ~ <소제목5>  (위와 동일한 형태로 반복, 최대 5개)  │
├─────────────────────────────────────────────┤
│ ※ 위 소제목 블록 전체는, 해당 회차에 수집된 기사가        │
│   하나도 없을 때만 전체 화면 형태로 "하나도 없어요"로     │
│   대체 표시됨 (소제목별 개별 빈 표시가 아님)              │
├─────────────────────────────────────────────┤
│ 🤖 AI가 추출한 주요 키워드                        │  ← [수정: 2026-07-24] "AI 추출한"→"AI가 추출한"
│  키워드1, 키워드2, 키워드3                         │  ← [수정: 2026-07-24] #태그 → 쉼표 나열
│                                                │
│ 💬 AI가 읽은 소제목별 주요 요약                     │
│  <소제목1> 요약문 (3문장·150자 이내)                 │
│  <소제목2> 요약문 ...                            │
├─────────────────────────────────────────────┤
│ 🏠 메인으로 가기                                  │  ← [추가: 2026-07-24] 진입 화면(home.html)으로
└─────────────────────────────────────────────┘

[추가: 2026-07-24] 기능 상세:
- **🗑️ 숨기기**: 눌러진 즉시 fetch로 서버(설정 서버)에 알리고, 성공하면 그 기사를 화면에서
  바로 지운다(새로고침 불필요). 원본 수집 데이터는 지우지 않고 "숨긴 기사 URL 목록"만 별도
  파일(`data/hidden_articles.json`)에 기록해, 화면·복사/내보내기 텍스트·워드클라우드 생성
  시점에 걸러낸다. 설정 화면 "🗑️ 숨긴 기사 관리"에서 언제든 되돌릴 수 있다.
- **↑/↓ 순서 조정**: 같은 소제목 안에서는 원본 기사 리스트의 두 위치를 맞바꿔 순서만 바꾼다.
  소제목의 맨 위/아래에서 한 번 더 누르면 바로 앞/뒤 소제목으로 기사가 통째로 옮겨간다(경계
  넘나들기) — 이건 "강제 소제목 배정"(`data/group_overrides.json`, {기사 url: 소제목 단어})을
  새로 만드는 것이라, 다음 렌더링에도 유지된다. 대상 소제목이 다음 회차 자동 분류에 없으면
  (관련 기사가 다 사라짐 등) 조용히 무시하고 자동 분류로 되돌아간다(자기 치유). 버튼은 맨
  처음 소제목의 첫 기사(↑)·맨 마지막 소제목의 마지막 기사(↓)일 때만 비활성화된다.
- **✏️ 소제목 이름 바꾸기**: `prompt()`로 새 이름을 받아 저장한다(`data/group_labels.json`,
  {원래 소제목 단어: 표시 이름}). 분류 로직은 항상 원래 단어 기준으로 그대로 동작하고, 이
  이름표는 화면·복사/내보내기 텍스트에 보여줄 때만 마지막에 적용된다. 원래 단어로 다시
  입력하면 이름표가 지워진다.
- 위 세 기능 모두 file://로 열리는 정적 화면에서 로컬 설정 서버(http://127.0.0.1:8765)로
  상태를 저장해야 해서, 자바스크립트 `fetch`(가벼운 앱 원칙의 예외 사례들)로 비동기 저장 후
  성공하면 DOM 반영 또는 `location.reload()`를 한다. 실패하면 알림을 띄운다.
- **🔄 새로고침 표시기**: 화면은 1분마다 자동 새로고침된다(`<meta http-equiv="refresh">`,
  자바스크립트 불필요). 표시기 텍스트는 마지막으로 화면이 만들어진 실제 시각이며, 새로고침될
  때마다 🔄 아이콘이 CSS `@keyframes` 애니메이션으로 1.2초간 회전한다. 표시기 텍스트는
  `user-select: none`으로 선택/복사되지 않는다.
- ↑/↓/🗑️/복사·내보내기·설정 버튼들은 모두 `user-select: none`이 걸려 있어, 마우스로 화면
  전체를 드래그해 선택·복사해도 버튼 글자는 섞여 나오지 않는다.

[📅 지난 기사 더보기] 클릭 시 별도 화면 (history.html, 최근 7일):
┌─────────────────────────────────────────────┐
│ ▶️ 당일 (07-23)                                 │  ← 날짜 토글 (details/summary)
│ ▶️ 07-22                                       │
│ ▶️ 07-21  ...                                  │
└─────────────────────────────────────────────┘
   날짜를 펼치면:
┌─────────────────────────────────────────────┐
│ ▼ 당일 (07-23)                                 │
│   ▶️ 1. 09:00                                  │  ← 시간대 토글 (details/summary)
│   ▶️ 2. 10:30                                  │
│   ▶️ 3. 13:30                                  │
│   ▶️ 4. 16:30                                  │
└─────────────────────────────────────────────┘
   시간대를 펼치면 그 회차의 기사 목록(ㅇ 언론사 제목 + URL, 클릭 시 요약)이 나온다.
   해당 회차에 기사가 없었으면 "하나도 없어요"만 보인다. 소제목 재분류는 하지 않고
   (당시 화면 그대로 보존할 필요는 없어 단순화) 언론사 우선순위 순 목록만 보여준다.
   [추가: 2026-07-24] 🗑️ 숨기기 버튼은 여기에도 있다(숨김은 전역 상태라 지난 기사에도
   반영되어야 하므로). 다만 ↑/↓ 순서 조정·✏️ 이름 바꾸기는 소제목 재분류가 없는 이
   화면에는 없다.

[설정] 클릭 시, 먼저 메뉴 화면으로 이동 ([수정: 2026-07-24] 이름에서 "설정" 군더더기를
빼고, 수집 관련(키워드·언론사·시간)/표시 관련(형광펜·본문 형식)/관리(숨긴 기사) 세
그룹으로 재배열, "출력 형식"→"본문 형식"으로 개명, 하위 페이지 4개→6개):
┌─────────────────────────────────────────────┐
│ ⚙️ 설정                                       │
│                                                │
│  🔎 검색 키워드                                  │  ← 클릭 시 아래 (a) 페이지로 이동
│  📰 주요 언론사 선택 및 순서 지정                     │  ← 클릭 시 아래 (b) 페이지로 이동
│  ⏰ 스크랩 시간 및 횟수                            │  ← 클릭 시 아래 (e) 페이지로 이동 [추가: 2026-07-24]
│  ─────────────                                │  ← 그룹 구분선
│  💡 형광펜 단어                                  │  ← 클릭 시 아래 (c) 페이지로 이동
│  🖊️ 본문 형식                                   │  ← 클릭 시 아래 (d) 페이지로 이동
│  ─────────────                                │  ← 그룹 구분선
│  🗑️ 숨긴 기사 관리                               │  ← 클릭 시 아래 (f) 페이지로 이동 [추가: 2026-07-24]
│                                                │
│  🏠 메인 화면으로                                 │  ← [수정: 2026-07-24] 진입 화면(home.html)으로
└─────────────────────────────────────────────┘

(a) 🔎 검색 키워드 [수정: 2026-07-24 — 제목에서 "설정" 뺌]:
┌─────────────────────────────────────────────┐
│ 검색 키워드 (최대 7개)                            │
│  [재정경제부] [x]                               │
│  [재경부]    [x]                               │
│  [+ 키워드 추가] (최대 7개까지)                     │
│                                                │
│  검색 방식: (●) OR - 하나라도 포함  ( ) AND - 모두 포함 │
│   ※ 키워드가 2개 이상 등록됐을 때만 표시                │
│                                                │
│  [저장]                                        │
└─────────────────────────────────────────────┘

(b) 📰 주요 언론사 선택 및 순서 지정:
┌─────────────────────────────────────────────┐
│ 📰 주요 언론사 (체크한 언론사만 수집, 안 고르면 기본 우선순위) │
│  스크랩 원하는 언론사를 클릭해주세요.                    │
│  <방송사> [ ]KBS [ ]MBC [ ]SBS [ ]YTN [ ]MBN        │
│  <전국종합일간> [ ]조선일보 [ ]동아일보 ... (지정된 순서) │
│  <경제일간> [ ]매일경제 [ ]한국경제 ...              │
│  <영자일간> <통신사> <전문일간> <기타 언론사> ...        │
│  [저장]                                        │
│                                                │
│  현재 선택 순서:                                  │
│   1. KBS            [↑][↓]                     │
│   2. 조선일보         [↑][↓]                     │
│   3. ...                                       │
└─────────────────────────────────────────────┘

(c) 💡 형광펜 단어 (검색 키워드와 완전히 별개):
┌─────────────────────────────────────────────┐
│ 💡 형광펜 단어                                   │
│  각 기사 아래 요약본에 아래 키워드가 있을 경우           │
│  형광펜 처리 됩니다.                               │
│  ※ 최대 3개까지 키워드 입력 가능                     │
│  [재정경제부]                                    │
│  [재경부]                                       │
│  [      ]                                     │
│  [저장]                                        │
└─────────────────────────────────────────────┘

(d) 🖊️ 본문 형식 [수정: 2026-07-24 — "🎨 출력 형식"에서 개명. "ㅇ (언론사) 기사제목" 줄의
기호만 커스텀, URL·날짜는 범위 밖):
┌─────────────────────────────────────────────┐
│ 🖊️ 본문 형식                                    │
│  기사 목록 첫 줄의 형식을 바꿀 수 있습니다.                │
│  {{outlet}}은 언론사명, {{title}}은 기사 제목으로 바뀝니다.  │
│  ※ 두 자리표시자를 각각 정확히 1번씩 포함해야 합니다.        │
│  [ㅇ ({{outlet}}) {{title}}                    ]  │  ← 기본값
│  미리보기: ㅇ (한겨레) 예시 기사 제목입니다               │
│  [저장]                                        │
└─────────────────────────────────────────────┘

(e) ⏰ 스크랩 시간 및 횟수 [추가: 2026-07-24 — 자동 실행 시각을 코드가 아니라 설정에서 편집]:
┌─────────────────────────────────────────────┐
│ ⏰ 스크랩 시간 및 횟수                             │
│  스크랩을 실행할 시각을 등록하세요.                     │
│  등록한 개수만큼 하루에 자동 실행됩니다                  │
│  (최소 1개, 최대 10개).                          │
│  [오전 09:00 🕐]                                │  ← 기본 4개만 노출, input type="time"
│  [오전 10:30 🕐]                                │     (형식 오류 자체가 안 남)
│  [오후 01:30 🕐]                                │
│  [오후 04:30 🕐]                                │
│  [+ 시간대 추가]                                  │  ← 누르면 입력하던 값 유지한 채 칸 하나 더 노출
│  [저장]                                        │     (자바스크립트 없이 formaction만 다르게 지정)
└─────────────────────────────────────────────┘
- 최대 10개로 상한을 둔 이유: 너무 잦으면(예: 5분 간격) 거의 같은 내용이 새 회차로 계속
  쌓여 "지난 기사" 목록만 어수선해지기 때문.
- 등록한 시각이 곧 헤더의 "언론 모니터링 [시각] 기준" 제목이 된다 — 코드 변경 불필요.

(f) 🗑️ 숨긴 기사 관리 [추가: 2026-07-24]:
┌─────────────────────────────────────────────┐
│ 🗑️ 숨긴 기사 관리                                │
│  화면의 🗑️ 버튼으로 숨긴 기사 목록입니다.               │
│  원본 데이터는 지워지지 않았으며,                      │
│  "되돌리기"를 누르면 다시 화면에 보이게 됩니다.           │
│  (한겨레) 예시 숨긴 기사 제목        [되돌리기]         │
│  ...                                          │
└─────────────────────────────────────────────┘
- 숨긴 기사 목록 자체는 URL만 저장하므로, 언론사·제목 표시를 위해 최근 7일 회차에서
  URL로 원본 정보를 찾아 보여준다(그 사이 7일 보관 기간이 지나 원본이 삭제됐으면 URL만 표시).

- 하위 6페이지 모두 저장 후에는 각자 자기 페이지로 되돌아온다(메뉴로 안 돌아감) —
  이어서 같은 항목을 더 손보기 편하도록.
- 언론사 순서는 드래그 앤 드롭이 아니라 "↑"/"↓" 버튼으로 바꾼다 — 가벼운 앱을 유지하기 위해
  자바스크립트 없는 폼 제출 방식(기존 설정 저장과 동일한 Post/Redirect/Get 패턴)을 그대로 쓴다.
- [추가: 2026-07-24] 설정 화면의 "메인 화면으로"/"메인 화면으로 가기" 링크는 처음엔
  `file://` 경로를 가리켰는데, 최신 브라우저(사파리 포함)가 보안상 http 페이지에서 file
  경로로의 이동을 막아 버튼이 먹통이었다. 그래서 설정 서버가 `index.html`/`history.html`/
  `home.html`·로고 파일도 같은 `http://127.0.0.1:8765` 출처로 함께 서빙하고, 이 링크들도
  file:// 대신 그 http 주소(구체적으로 `/home.html` — 진입 화면)를 가리키도록 바꿨다. 앱을
  처음 켤 때 열리는 화면(file://, `main.py`)은 이 변경과 무관하게 그대로다.

- 색상: [수정: 2026-07-24] 어두운 남색 배경(구버전)이 링크 등에서 가독성이 떨어져 밝은 카드형
  테마로 전환 — 배경 `#F8FAFC`, 카드 `#FFFFFF`, 헤더/제목 `#1E3A5F`, 포인트(링크·버튼) `#2563EB`,
  본문 `#1F2937`, 보조텍스트 `#6B7280`, 라인/구분선 `#E5E7EB`, hover 배경 `#EFF6FF`, 오류 문구
  `#DC2626` (`app/config.py`의 `COLOR_*` 상수로 중앙 관리). 요약 속 형광펜 단어(검색 키워드
  아님, 위 (c) 참고)는 `#C6FF00` 형광 연두색 배경 + 짙은 글자색(`COLOR_TEXT`) 하이라이트.
- 글꼴: `Pretendard` 우선, 없으면 `Noto Sans KR`, 그래도 없으면 시스템 기본 순으로 대체
  (CSS `font-family` 선언만으로 처리 — 폰트 파일을 따로 내려받거나 CDN에 의존하지 않음).
- 화면은 PC 위주지만 모바일에서도 세로로 읽히도록 반응형(폭에 따라 1열로 쌓임).
- [수정: 2026-07-23] 메인 화면은 항상 최신 회차를 기본 표시하되, 헤더 액션 버튼의 "📅 지난 기사 더보기"로 최근 7일간 회차를 날짜별 → 시간대별로 조회할 수 있다 (7일 보관 정책이 지나면 목록에서도 사라짐). 최초 결정("지난 회차 조회 없음")은 이후 요청으로 변경됨.
- [수정: 2026-07-24] 메인 화면은 이제 1분마다 자동 새로고침된다(`<meta http-equiv="refresh">`). 새 회차가 생기면 열려 있던 탭도 최대 1분 안에 자동으로 반영된다 — 이전 버전(정적 HTML이라 자동 새로고침 없음)에서 실무자 요청으로 변경. 🗑️/↑/↓/✏️ 조작은 클릭 즉시 서버에 저장한 뒤 화면을 바꾸는 구조라 새로고침 타이밍이 겹쳐도 잃어버릴 편집 상태가 없으며, 숨긴 기사는 화면 생성 시점에 아예 제외되므로 새로고침을 거듭해도 다시 나타나지 않는다.

## 2. 데이터 흐름 (입력 → 처리 → 출력)

```
[스케줄 트리거]
  기본값 09:00 / 10:30 / 13:30 / 16:30 자동 실행 — [수정: 2026-07-24] 이 시각·횟수는
  설정 화면 "⏰ 스크랩 시간 및 횟수"에서 최소 1개~최대 10개까지 자유롭게 편집 가능
  (`settings.json`의 `schedule_times` 필드, 코드 상수가 아님)
  (컴퓨터가 잠자기 상태였던 동안에는 정각 실행이 되지 않으며,
   앱 실행 시 또는 컴퓨터가 깨어난 직후 가장 최근에 놓친 회차 1개만 즉시 보충 실행.
   놓친 회차가 여러 개여도 가장 최근 것만 실행하고 더 이전 회차는 건너뜀)
        │
        ▼
[입력] 네이버 뉴스 검색 API 호출
  - 등록된 키워드(기본 "재정경제부","재경부")로 검색, 2개 이상이면 설정에서 고른 OR/AND 방식 적용
  - 게시일이 당일(한국 시간)인 기사만 수집. API가 최신순(sort=date)으로 주므로, 오늘보다
    오래된 기사가 나오는 순간 그 키워드의 다음 페이지 조회를 그만둔다 (API 호출 절약)
  - 검색은 기사 원문(본문) 전체를 대상으로 한다 — API가 돌려주는 description은 그 중
    일부를 보여주는 발췌일 뿐, 매칭 범위 자체가 아니다.
        │
        ▼
[처리 1] 정렬
  - 방송사(KBS, MBC) → 주요 언론사 목록 → 그 외 순 (설정에서 언론사 화이트리스트를
    지정했으면 그 순서·목록을 대신 따름)
        │
        ▼
[처리 2] 필터링
  - [수정: 2026-07-24] 제목에 사진기사 힌트 단어(`포토`/`[포토]`/`사진`/`화보`/`포토뉴스`/
    `PHOTO`, 대소문자 무시) 중 하나라도 포함 → 제외. 단 `[속보]`가 붙어 있으면 다른 힌트
    단어가 같이 있어도 예외로 항상 수집 (기존엔 `[포토]` 프리픽스만 봤음)
  - 제목이 완전히 동일한 기사 → 1건만 남김 (먼저 나온 것을 남기므로,
    정렬을 먼저 해야 우선순위 높은 언론사의 사본이 살아남는다)
        │
        ▼
[처리 3] 분류/추출 (규칙 기반, 별도 AI API 없음)
  - [수정: 2026-07-24] 기사 제목이 아니라 네이버 요약(description)에 자주 나오는 단어를
    묶어 소제목 최대 5개 생성 (언론사마다 표현이 제각각인 제목보다, 보도자료를 비슷하게
    옮겨쓴 요약문에서 공통 단어가 더 잘 드러남. 요약이 비어 있으면 제목으로 대신함)
  - 기사를 소제목 1곳에 배정. [추가: 2026-07-24] 담당자가 ↑/↓ 버튼으로 소제목 경계를
    넘겨 수동으로 재배정할 수 있으며, 이 수동 배정이 자동 분류보다 우선함(대상 소제목이
    사라지면 자동으로 원래 방식에 되돌아감)
  - 소제목별 요약(150자↓), 전체 키워드 추출 — [수정: 2026-07-24] 전체 키워드 표시 형식이
    `#태그` 나열에서 쉼표 나열(`키워드1, 키워드2, ...`)로 변경
  - 요약 속 "형광펜 단어"(검색 키워드와 별개로 설정된 하이라이트 전용 단어) 마킹
  - [추가: 2026-07-24] 담당자가 ✏️ 버튼으로 소제목의 표시 이름을 바꿀 수 있음(분류 자체는
    영향 없음, 화면에 보여줄 때만 적용)
        │
        ▼
[저장] 이번 회차 결과를 로컬 데이터 파일에 저장
  - 과거 7일치까지만 보관, 이후 자동 삭제
  - [추가: 2026-07-24] 이 외에도 "숨긴 기사 URL 목록"(`hidden_articles.json`), "소제목
    경계 넘나들기 강제 배정"(`group_overrides.json`), "소제목 이름표"(`group_labels.json`)
    등 사용자가 화면에서 직접 조작한 상태도 함께 로컬 파일로 보관됨
        │
        ▼
[출력] 저장된 최신 회차 데이터를 화면 파일로 변환 (숨긴 기사는 이 단계에서 걸러짐)
  - 브라우저가 자동으로 열려 결과 화면 표시
  - [추가: 2026-07-24] 메인 화면은 이후 1분마다 자동 새로고침되어, 열려 있던 탭도 새
    회차를 스스로 반영함
        │
        ▼
[사용자 액션]
  - 클립보드 복사 / .txt 내보내기
  - 기사 제목 클릭 → 요약 보기 / URL 클릭 → 원문 새 탭
  - [추가: 2026-07-24] 🗑️ 기사 숨기기/되돌리기, ↑/↓ 순서 조정(소제목 경계 넘나들기 포함),
    ✏️ 소제목 이름 바꾸기
  - 설정 화면에서 키워드·언론사·형광펜 단어·출력 형식·스크랩 시간 추가·수정·삭제 →
    저장 시 해당 설정 파일 갱신
```

- 실패(네트워크 오류) 시: 5분 간격으로 최대 3회까지 [입력] 단계부터 재시도.

## 3. 기술 선택

| 난이도 | 방식 | 한 줄 설명 |
|--------|------|-----------|
| 쉬움 | 파일 기반 | 서버 없이 파이썬 스크립트가 결과를 HTML 파일로 만들고, 그 파일을 브라우저로 열어 보는 방식 |
| 보통 | 웹서버 방식 (PRD 원안) | 파이썬 FastAPI 서버 + SQLite DB로 만들어 `localhost` 주소로 실시간 접속하는 방식 |
| 고급 | 프론트엔드 분리 방식 | 같은 서버에 React 등 화면 전용 도구를 더해 화면을 따로 만드는 방식 |

**선택: 쉬움 (파일 기반 방식)**

- 스크랩·분류 로직은 순수 파이썬 스크립트로 작성하고, 결과를 정적 HTML 파일(`index.html`)로 새로 생성한다.
- 하루 4회 스케줄과 "꺼져 있던 동안 놓친 회차 즉시 실행"은, 앱을 켜두면 계속 떠 있는 가벼운 파이썬 스케줄 루프(백그라운드 대기 스크립트) 하나가 담당한다. 별도 웹 프레임워크(FastAPI 등)는 쓰지 않는다.
  - 단, 이 스케줄 루프는 컴퓨터가 잠자기 상태인 동안에는 함께 멈춘다. 따라서 예약 시각 "정각" 실행은 보장되지 않으며, 컴퓨터가 깨어난 직후 놓친 회차를 즉시 보충 실행하는 방식으로 대체한다(원안 PRD의 APScheduler 방식도 동일한 한계가 있었음).
- 데이터 저장은 SQLite 대신 로컬 JSON 파일로 하며, 7일이 지난 기록은 스케줄 루프가 알아서 지운다.
- 네이버 API 키(`NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`)는 스크립트 시작 시 `.env` 파일에서 불러와 사용하고, 코드나 저장소에는 직접 노출하지 않는다.
- **예외 1곳, [수정: 2026-07-24] 처음보다 커짐**: "설정 화면에서 키워드를 코드 수정 없이 저장"하려면 브라우저가 파일에 다시 쓸 방법이 필요하므로, 파이썬 내장 `http.server`(별도 프레임워크 없음) 기반 로컬 서버를 하나 둔다. 처음엔 "저장 버튼 눌렀을 때만 동작하는 아주 작은 스크립트"였지만, 지금은 설정 6페이지의 GET/POST 전부와 기사 숨김·순서 조정·소제목 이름 바꾸기 API(`/hide-article`, `/move-article`, `/rename-group` 등), 그리고 `index.html`/`history.html`/`home.html`·로고 파일의 정적 서빙까지 상시 처리하는, 처음보다 훨씬 커진 로컬 서버다. 이 부분만 "서버"라고 부를 만한 유일한 조각이라는 성격은 그대로다. **인증은 여전히 없다** — "127.0.0.1(내 컴퓨터)에서만 접속 가능"이 전제라 의도적으로 만들지 않았다. 이 앱을 인터넷에 공개 배포하면 이 전제가 깨져 누구나 로그인 없이 설정을 바꿀 수 있게 되므로, 그 전에는 배포하면 안 된다(2026-07-24 논의).
- [추가: 2026-07-23] 출력 형식 템플릿(`{{outlet}}`/`{{title}}`)은 파이썬 `str.format()`이 아니라
  단순 문자열 치환(`.replace()`)으로 처리한다 — 사용자가 입력한 템플릿이나 기사 제목에 우연히
  중괄호가 들어 있어도 `str.format()`처럼 깨지거나 예상 밖의 값이 노출될 위험이 없다.
- [추가: 2026-07-23, 수정: 2026-07-24] 진입 화면의 워드클라우드는 차트 라이브러리 없이 CSS
  `font-size`+절대 위치(`position: absolute`, 삼각함수로 계산한 %)만으로 구현한다 — "가벼운 앱"
  원칙과 일치하며, 정확한 통계가 필요한 게 아니라 눈요기용이라 이 정도로 충분하다. (처음엔
  단순 가로 나열이었으나, 무궁화를 연상시키는 방사형 배치로 확장됨 — §0 참고)
- [추가: 2026-07-24] 스케줄 파일 쓰기 경쟁 방지: 스케줄러 스레드(정기 재생성)와 설정 서버
  스레드(숨김/순서 조정 등 조작 후 재생성)가 동시에 같은 정적 화면 파일에 쓸 수 있어,
  `app/atomic_write.py`(임시 파일에 쓴 뒤 교체) 하나로 모든 파일 쓰기를 통일했다.
- [추가: 2026-07-24] 자바스크립트는 여전히 "꼭 필요한 곳만" 원칙을 지키되, 허용 예외가
  4가지로 늘었다: ① 클립보드 복사, ② 🗑️ 기사 숨기기(`fetch`), ③ ↑/↓ 순서 조정(`fetch`),
  ④ ✏️ 소제목 이름 바꾸기(`prompt()` + `fetch`). 모두 file://로 열리는 정적 화면에서 로컬
  서버로 상태를 저장해야 해서 생긴 예외이며, 폼 제출만으로는(POST/Redirect/Get) 안 되는 이유는
  file:// 오리진에서 발생한 요청은 브라우저가 리다이렉트 응답을 그대로 file://로 돌려보내는
  걸 보안상 막기 때문이다(§1의 "메인 화면으로" 버그와 근본 원인이 같음).

## Version History

| Version | Date | Changes |
|---------|------|---------|
| 0.1 | 2026-07-23 | 초안 작성 (파일 기반 방식 선택) |
| 0.2 | 2026-07-23 | design-validator 검토 반영: 절전모드 실행 한계 명시, 정적 화면 새로고침 한계 명시, `.env` 키 로드 방식 명시, "하나도 없어요" 표시 범위 명확화 |
| 0.3 | 2026-07-23 | 검색 키워드 최대 7개로 확대, 하이라이트 단어를 검색 키워드에서 분리한 별도 설정으로 변경, 설정 화면을 메뉴+하위 3페이지 구조로 재편, Pretendard/Noto Sans KR 폰트 적용 |
| 0.4 | 2026-07-23 | 진입 화면(로고+제목+당일 키워드 워드클라우드+바로가기) 추가, 기사 출력 줄 형식 커스터마이징(기호만) 추가 |
| 0.5 | 2026-07-24 | 대규모 갱신 — 라이트 카드 테마로 전면 교체, 사진기사 제외 규칙 확대, 기사 숨김/되돌리기, ↑/↓ 순서 조정(소제목 경계 넘나들기 포함), 소제목 이름 바꾸기, 소제목 분류 기준 제목→요약 변경, 주요 키워드 #태그→쉼표 나열, 스크랩 시간/횟수 설정 UI, 메인 화면 1분 자동 새로고침+갱신 시각 표시기, 워드클라우드 무궁화 방사형 재구현(제목→요약, 로고 png→svg), 설정 서버가 정적 화면도 함께 서빙하도록 확장(브라우저 file:// 이동 제한 우회), 원자적 파일 쓰기 도입. 인터넷 배포(Vercel 등) 관련 아키텍처 제약(무한루프 스케줄러·로컬 파일 저장·인증 없는 설정 서버) 논의 |
