# PLAN.md

> PRD.md에 정의된 기능 전체를 구현하기 위한 PDCA Plan 문서입니다.

## 이번 사이클 목표

PRD의 Must 기능(기사 자동 스크랩 + 소제목 자동 분류)을, 서버 프레임워크 없이 파이썬 스크립트가 정적 화면 파일을 생성하는 방식으로 구현하여, 하루 4회(09:00/10:30/13:30/16:30) 자동 실행되게 하고, 담당자가 수작업 없이 브라우저에서 언론사·소제목별로 정리된 기사와 요약을 확인할 수 있게 한다.

## 성공 기준

- 09:00 / 10:30 / 13:30 / 16:30에 자동 스크랩이 실행된다. 단, 컴퓨터가 잠자기 모드였던 동안에는 정각 실행이 되지 않으며, 앱이 꺼져 있었거나 잠자기 상태였던 동안 놓친 회차는 앱 실행/컴퓨터가 깨어난 직후 즉시 보충 실행된다.
- 키워드 OR 매칭, `[포토]` 기사 제외, 완전 동일 제목 중복 제거, 언론사 우선순위(방송사 → 주요 언론사 → 기타) 정렬이 규칙대로 동작한다.
- 화면에서 소제목별(최대 5개) 분류·요약·키워드·하이라이트가 보이고, 클립보드 복사와 `.txt` 내보내기가 된다.
- 네트워크 오류 시 5분 간격 최대 3회 재시도, 저장된 기사는 7일 후 자동 삭제된다.

## 작업 순서 (먼저 되면 나머지가 쉬워지는 것부터)

1. 프로젝트 기본 구조 설정 (Python 가상환경, 스크립트 폴더 구성, `.env`로 네이버 API 키 로드)
2. 기사 저장용 로컬 JSON 파일 구조 설계
3. 네이버 뉴스 검색 API 연동 및 키워드 목록 검색 기능 구현 (키워드 2개 이상 시 OR/AND 방식 선택 가능, 기본값 OR; 당일(한국 시간) 게시 기사만 수집)
4. 검색 결과에서 `[포토]` 기사 제외, 완전 동일 제목 기사 중복 제거 처리
5. 언론사 우선순위(방송사 → 주요 언론사 → 그 외) 정렬 기능 구현
6. 언론사·제목·URL·요약(description)을 로컬 JSON 파일에 저장하는 기능 구현
7. 저장된 요약에서 일치하는 키워드를 `#C6FF00`으로 하이라이트 처리
8. 스크랩 실패 시 5분 간격 최대 3회 자동 재시도 로직 구현
9. 파이썬 스케줄 루프로 09:00/10:30/13:30/16:30 자동 실행 예약 구현 (잠자기 모드 중에는 실행되지 않으며, 컴퓨터가 깨어나거나 앱이 켜진 직후 가장 최근에 놓친 회차 1개만 즉시 보충 실행, 더 이전에 놓친 회차는 건너뜀)
10. 기사 제목을 분석해 소제목(최대 5개)을 자동 생성하고 기사를 배정하는 기능 구현 (규칙 기반, 외부 LLM API 없음)
11. 소제목별 요약(3문장 이내·150자 이내)과 전체 키워드(`#키워드`) 자동 추출 기능 구현
12. 저장 7일 경과 기사를 자동 삭제하는 배치 기능 구현
13. 소제목별 결과를 보여주는 웹 화면(파란 톤) 구현, 화면 맨 위에 "언론 모니터링 [실행 시각] 기준" 헤더 표시, 항상 최신 회차 1개만 표시하며 해당 회차 수집 기사가 하나도 없으면 전체 화면에 "하나도 없어요" 표시
14. 기사 제목 클릭 시 요약 보기, URL 클릭 시 새 탭에서 원문 이동 기능 구현
15. 소제목별 결과를 클립보드로 복사하거나 `.txt` 파일로 내보내는 기능 구현
16. 검색 키워드(3~4개)를 등록·수정·삭제할 수 있는 설정 화면 구현, 키워드가 2개 이상이면 OR/AND 선택 항목도 함께 표시
17. 전체 흐름(스크랩 → 분류 → 화면 표시 → 복사/내보내기) 통합 테스트 및 PRD 규칙 최종 검증
18. [추가: 2026-07-23] 메인 화면 하단 "📅 지난 기사 더보기" 링크로, 최근 7일간 저장된 회차를 날짜별 → 시간대별 토글로 조회하는 페이지 구현 (7일 보관 정책과 연동)
19. [추가: 2026-07-23] 설정 화면에 "언론사 선택" 기능 구현 — 카테고리별 체크박스로 언론사를 고르면 그 언론사만 수집 대상으로 남기고(화이트리스트), 선택된 언론사를 "↑"/"↓" 버튼으로 순서 조정 가능 (드래그 앤 드롭 대신 가벼운 방식)
20. [추가: 2026-07-23] ① 검색 키워드 최대 개수를 4개→7개로 확대, ② 하이라이트 대상 키워드를 검색 키워드에서 완전히 분리해 별도 설정("💡 형광펜 단어", 최대 3개)으로 구현, ③ 설정 화면을 "⚙️ 설정페이지" 메뉴 + 하위 3페이지(검색 키워드/언론사 선택/형광펜 단어) 구조로 재편, ④ 전체 화면 폰트를 Pretendard(→ 없으면 Noto Sans KR) 우선 적용
21. [추가: 2026-07-23] ① 기사 출력 줄 형식("ㅇ (언론사) 기사제목")을 설정 화면에서 `{outlet}`/`{title}` 자리표시자 템플릿으로 바꿀 수 있게 구현 (URL 줄·날짜 미표시는 그대로 유지, 이번 범위는 기호만), ② 앱 진입 화면(로고+제목+당일 키워드 워드클라우드+스크랩 바로가기+설정+문의) 신규 구현, `main.py`가 이 화면을 먼저 열도록 변경
22. [추가: 2026-07-24] 기사별 🗑️ 숨김 버튼 구현 — 원본 데이터는 보존한 채 "숨긴 기사 URL 목록"(`data/hidden_articles.json`)만 따로 관리하고, 화면(메인·지난 기사)·복사/내보내기 텍스트·워드클라우드에서 숨긴 기사를 제외. file://로 열리는 정적 화면에서 로컬 설정 서버로 상태를 저장해야 해서, `fetch`(자바스크립트 예외 2번째 허용 사례, CORS 허용 헤더 포함)로 저장 성공을 확인한 뒤에만 DOM에서 제거하고 실패하면 알림 표시. 설정 메뉴에 "🗑️ 숨긴 기사 관리" 하위 페이지를 추가해 숨긴 기사를 보고 "되돌리기"로 복원 가능. 정적 화면 파일은 스케줄러 스레드와 설정 서버 스레드가 동시에 쓸 수 있어 원자적 쓰기(`app/atomic_write.py`)로 교체
23. [추가: 2026-07-24] 자동 실행 시각/횟수를 설정 화면 "⏰ 스크랩 시간 및 횟수 설정"에서 자유롭게 편집하게 구현 — `settings.json`에 `schedule_times` 필드 추가(기본값: 기존 4개), `scheduler.py`의 각 함수가 고정 상수 대신 매 tick마다 `load_settings()`로 새로 읽도록 변경(재시작 없이 반영), `input type="time"` 태그로 형식 오류 자체를 방지, 최대 10개로 상한
24. [추가: 2026-07-24] 사진기사 제외 규칙 확대 — 기존 `[포토]` 프리픽스 전용에서 `PHOTO_HINT_WORDS`(포토·사진·화보·포토뉴스·PHOTO 등, 대소문자 무시) 목록으로 넓히고, `[속보]`가 붙은 제목은 다른 힌트 단어가 있어도 항상 예외로 수집. 본문(요약) 길이·문장수 기반 2차 필터는 이 앱이 요약만 갖고 있어 진짜 뉴스도 같이 걸러질 오탐 위험이 커서 보류(사용자와 확인 후 결정)
25. [추가: 2026-07-24] 설정 메뉴 재구성(이름에서 "설정" 군더더기 제거, 수집/표시/관리 3그룹으로 재배열, "출력 형식"→"본문 형식") + 전체 색상 팔레트를 어두운 남색 테마에서 밝은 카드형 테마로 전면 교체(`app/config.py`의 `COLOR_*` 상수로 중앙 관리, 설정 6페이지+메인/지난기사/진입화면 전부 적용) — 기존 테마의 링크 가독성 문제 해결
26. [추가: 2026-07-24, 수정: 2026-07-24] 메인 화면 기사에 ↑/↓ 순서 조정 버튼 구현 — `app/curation.py`의 `move_article()`이 같은 소제목 안에서는 원본 기사 리스트의 두 위치를 맞바꾸고, 소제목 경계에서는 "강제 소제목 배정"(`data/group_overrides.json`, `set_group_override`)을 새로 만들어 옆 소제목으로 통째로 넘어가게 함(규칙21 수정). `app/classifier.py`의 `classify_articles()`에 `forced_groups` 인자를 추가해 자동 분류 뒤 마지막에 강제 배정을 적용하며, 대상 소제목이 사라지면 자동으로 무시(자기 치유). `app/storage.py`의 `update_run_articles()`로 기존 회차 파일에 순서 변경만 덮어씀. 🗑️ 버튼과 동일하게 fetch로 로컬 서버에 알리고 성공 시 `location.reload()`로 갱신된 화면을 다시 불러옴
27. [추가: 2026-07-24] 소제목 분류 기준을 기사 제목에서 네이버 요약(description)으로 변경 — 언론사마다 제각각인 제목보다 보도자료를 옮긴 요약문에 공통 단어("○○회의" 등)가 더 잘 잡혀 소제목이 지나치게 좁게 쪼개지는 문제를 완화. `app/tokenizer.py`의 STOPWORDS에 요약문에 흔한 보도체 상투어(밝혔다/전했다/이날 등) 추가, 날짜·시각 패턴("24일" 등)도 정규식으로 자동 제외. 실제 운영 결과 보며 계속 다듬기로 함
28. [추가: 2026-07-24] 메인 화면(index.html) 버튼 배치 변경 — "📅 지난 기사 더보기"를 화면 하단에서 상단 버튼 영역(복사·내보내기·설정 옆)으로 옮기고, 대신 하단에 "🏠 메인으로 가기"(진입 화면 home.html로 이동) 추가
29. [추가: 2026-07-24] 메인 화면 3분 자동 새로고침 구현 — `<meta http-equiv="refresh" content="180">` 한 줄로 처리(자바스크립트·폴링 로직 불필요). 실시간 편집(🗑️/↑/↓)과 충돌하지 않는 이유: 모든 조작이 클릭 즉시 서버 저장 → 재생성 순서라 "저장 안 된 상태"가 없음
31. [추가: 2026-07-24] 워드클라우드를 무궁화 연상 방사형 배치로 재구현 — 배경 그림 없이 1위 단어는 가운데 진한 빨강·큰 글자, 2~4위는 안쪽 고리 금색(순수 노랑은 밝은 배경에서 안 보여 다크골드로 대체), 나머지는 바깥 고리 분홍 계열로 삼각함수 기반 위치 계산(`app/landing_renderer.py`). 단어 추출 기준도 제목→요약(description)으로 변경, "당일 주요 키워드" 문구 제거, 로고 회색 테두리 제거
30. [추가: 2026-07-24] 소제목 이름 바꾸기 구현 — `app/curation.py`에 `set_group_label()`/`load_group_labels()`/`display_group_name()` 추가(`data/group_labels.json`, {원래 소제목 단어: 표시 이름}). 분류·강제 배정(move_article) 로직은 항상 원래 단어 기준으로 그대로 두고, 렌더링 마지막 단계에서만 이름표를 적용해 화면·복사/내보내기 텍스트에 반영. 소제목 옆 ✏️ 버튼이 `prompt()`로 새 이름을 받아 fetch로 저장(자바스크립트 예외 4번째 허용 사례), 원래 단어로 되돌리면 이름표 자동 삭제
32. [수정: 2026-07-24] 버그 수정: 설정 화면의 "메인 화면으로" 링크가 실제 브라우저(사파리 등)에서 안 눌림 — http(설정 서버) 페이지에서 file://(정적 화면) 경로로의 이동을 최신 브라우저가 보안상 차단하기 때문이었다. 설정 서버(`app/settings_server.py`)가 index.html/history.html/home.html·로고 파일도 같은 127.0.0.1 출처로 함께 서빙하도록 하고, 이 링크가 file:// 대신 그 http 주소를 가리키게 바꿔 해결. 앱을 처음 열 때(file://, `main.py`)의 기존 동작은 그대로 유지
