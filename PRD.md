# 재정경제부 언론 모니터링 자동화 서비스 기획서 (PRD)

- 작성자: [홍정희]
- 작성일: (2026.7.22.)

---

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

언론 모니터링 담당자는 네이버 뉴스에 게재되는 재정경제부 관련 기사를 하루에도 여러 차례 직접 검색해 확인하고, 언론사·제목·URL을 메모장에 일일이 옮겨 붙이는 방식으로 정리해왔다. 이 과정은 반복적인 수작업이 많고, 기사를 놓치거나 정리에 많은 시간이 소요되는 불편이 있다.

---

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

- 현행 방식: 담당자가 하루 중 필요한 시점마다 네이버 뉴스에서 재정경제부 관련 기사를 직접 검색하고, 관련 기사를 하나씩 열어 언론사·제목·URL을 확인한 뒤 메모장에 수기로 옮겨 적는다. 기사 내용을 소제목별로 분류하거나 요약하는 것도 담당자가 직접 판단해서 정리한다.
- 한계:
  - 검색·정리에 반복적인 시간이 소요된다.
  - 사람이 직접 확인하다 보니 기사를 놓치거나 중복 정리할 위험이 있다.
  - 언론사 우선순위, 시간대별 확인 등을 매번 사람이 판단해야 해서 일관성이 떨어진다.
  - 정리 결과가 개인 메모장에만 남아 형식이 통일되지 않고 재사용·검색이 어렵다.

---

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

- 기사 스크랩·정리에 드는 수작업 시간을 대폭 절감 (별도 수치 측정은 하지 않음)
- 매일 09:00 / 10:30 / 13:30 / 16:30, 하루 4회 자동 수집으로 "재정경제부" 또는 "재경부" 키워드가 포함된 기사를 모두 수집 (검색 결과 기준 전수)
- 기사 원문 요약(API description)과 소제목별 요약(150자 이내)·키워드 자동 추출로 담당자의 내용 파악 시간 단축

---

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

- 사용자: 언론 모니터링 담당자 (단일 사용자, 로그인 없이 로컬에서 사용)
- 실행 방식: 사용자가 앱을 실행해두면 동작함. 컴퓨터가 잠자기 모드였던 동안에는 예약 시각에 실행되지 않으며, 컴퓨터가 깨어난 직후 놓친 회차를 즉시 보충 실행함. 앱 실행 시 브라우저가 자동으로 열려 [수정: 2026-07-23] 진입 화면(기능3)으로 연결되며, 거기서 "스크랩 보러 가기"를 눌러 실제 결과 화면으로 이동함
- 이용 흐름:
  언론사 우선순위·수집 시간대 사전 설정 → 매일 09:00 / 10:30 / 13:30 / 16:30 자동 스크랩 실행 → 재정경제부 관련 기사 자동 수집 및 소제목 자동 분류 → 담당자가 화면에서 소제목별로 정리된 기사·키워드·요약 확인 → 필요 시 기사 원문(하이라이트 포함) 열람 후 업무에 활용

---

## 5. 주요 기능 (must / nice로 구분)

**Must**

1) 기사 자동 스크랩
- 설명: 네이버 뉴스 검색 API를 통해 재정경제부 관련 기사를 자동으로 수집하여 메모장 형식으로 변환
- AI가 꼭 지켜야 할 규칙:
  1. 매일 오전 9시 / 오전 10시 30분 / 오후 1시 30분 / 오후 4시 30분, 하루 4회 자동 실행 — [수정: 2026-07-24] 이 시각과 횟수는 기본값이며, 설정 화면에서 자유롭게 추가·삭제·변경할 수 있다 (규칙20)
  2. 네이버 뉴스 검색 API를 통해, 설정 화면에 등록된 키워드 목록(기본값: "재정경제부", "재경부")으로 검색 결과를 수집 (건수 제한 없음). 키워드가 2개 이상이면 OR(하나라도 포함, 기본값) / AND(모두 포함) 중 검색 방식을 설정 화면에서 선택할 수 있으며, 키워드가 1개뿐이면 이 선택지는 의미가 없으므로 표시하지 않음. 게시일이 당일(한국 시간 기준)인 기사만 수집하고, 지난 날짜 기사는 수집 대상에서 제외함 (화면에 게시일자를 표시하지는 않으며, 수집 시 내부 판단 기준으로만 사용)
  3. 언론사 우선순위: ① 방송사(KBS, MBC) → ② 주요 언론사(조선일보, 중앙일보, 동아일보, 한국경제, 매일경제, 서울경제, 한국일보, 머니투데이, 이데일리, 연합뉴스, 뉴시스 등) 순으로 정렬하며, 목록에 없는 언론사는 제외하지 않고 맨 뒤에 붙여서 포함 — 단, [수정: 2026-07-23] 설정 화면에서 "언론사 선택"을 하나 이상 지정하면, 그 순간부터는 이 기본 우선순위 대신 사용자가 고른 언론사만 수집 대상으로 남기고(선택 안 한 언론사는 완전히 제외), 사용자가 지정한 순서대로 정렬한다. 아무것도 선택하지 않으면 기존 기본 동작(전체 포함) 유지
  4. 동일 기사가 여러 회차나 언론사에 중복 게재되어도 별도로 제거하지 않고 모두 수집하되, 기사 제목이 완전히 동일한 경우(주로 [포토] 등 사진 기사에서 발생)에는 1개만 수집
  5. 출력 형식은 다음과 같고, 게시일자는 표시하지 않음
     ```
     ㅇ (언론사) 기사제목
     URL
     ```
     [수정: 2026-07-23] 첫 줄의 형식("ㅇ (언론사) 기사제목" 부분)은 설정 화면에서 `{outlet}`·`{title}` 자리표시자가 들어간 문구로 바꿀 수 있다 (예: 기본값 `ㅇ ({outlet}) {title}` → `[{outlet}] {title}`로 변경). URL 줄과 게시일자 미표시는 이 커스터마이징 범위 밖으로, 그대로 유지된다
  6. 기사 본문 전체를 크롤링하지 않고, 네이버 검색 API가 제공하는 간단한 요약(description)만 저장. 저장된 요약 중 일치하는 키워드는 형광 연두색(#C6FF00)으로 하이라이트 표시 — [수정: 2026-07-23] 하이라이트 대상 키워드는 검색 키워드(규칙2)와 완전히 별개이며, 설정 화면에서 최대 3개까지 직접 지정한다 (기본값: "재정경제부", "재경부")
  7. 해당 회차에 수집된 기사가 없으면 화면에 "하나도 없어요" 문구 표시
  8. 네트워크 오류 등으로 스크랩 실패 시 5분 간격으로 최대 3회 자동 재시도
  9. 앱이 꺼져 있어 지나가버린 스크랩 시간대가 있으면, 앱을 켜자마자 가장 최근에 놓친 회차 1개만 바로 실행한다. 놓친 회차가 여러 개여도(예: 09:00·10:30을 모두 미실행하고 11시에 앱 실행) 가장 최근 것(10:30)만 실행하며, 더 이전 회차는 실행하지 않는다
  10. 화면에 표시된 소제목별 스크랩 목록(언론사·기사제목·URL)을 클립보드로 복사하거나, 메모장 형식의 .txt 파일로 내보낼 수 있는 기능 제공
  11. 화면·내보내기 결과 맨 위에 "언론 모니터링 [스크랩 실행 시각] 기준" 문구를 헤더로 표시 (예: 09:00 회차라면 "언론 모니터링 09:00 기준")
  12. 제목에 사진기사 힌트 단어(`포토`, `[포토]`, `사진`, `화보`, `포토뉴스`, `PHOTO`, 대소문자 무시)가 하나라도 포함되면 수집 대상에서 아예 제외 — [수정: 2026-07-24] 기존 `[포토]` 프리픽스 전용 규칙에서 확대. 단, 제목에 `[속보]`가 붙어 있으면 다른 힌트 단어가 같이 있어도 예외로 항상 수집(단신이어도 속보는 놓치지 않음). 본문 길이·문장수 기반 2차 필터는 이 앱이 네이버 API의 짧은 요약(description)만 갖고 있어 진짜 뉴스도 함께 걸러질 위험이 커서 도입하지 않기로 함(오탐 방지 우선)
  13. 화면에서 기사 제목을 클릭하면 저장된 요약(API description)을 볼 수 있음
  14. 화면에서 기사 URL을 클릭하면 원본 네이버 뉴스 기사 페이지로 새 탭에서 이동
  15. [수정: 2026-07-23] 설정 화면에서 검색 키워드를 최대 7개까지 등록·수정·삭제할 수 있으며, 코드 수정 없이 화면에서 바로 변경 가능 (기본값: "재정경제부", "재경부"). 키워드가 2개 이상 등록되면 OR/AND 검색 방식 선택 항목도 함께 표시 (기본값 OR)
  16. [추가: 2026-07-23, 순서 갱신: 2026-07-23] 설정 화면에서 "언론사 선택" 항목을 두어, 방송사·전국종합일간·경제일간·영자일간·통신사·전문일간·기타 언론사로 분류된 언론사 목록(지정된 순서, OBS·이투데이·내일신문은 제외) 중 원하는 곳을 체크박스로 고를 수 있다. 고른 언론사는 화면에 순서대로(선택된 순) 나열되며, 각 항목 옆 "↑"/"↓" 버튼으로 순서를 바꿀 수 있다 (드래그 앤 드롭 대신 가벼운 방식 채택). 아무것도 선택하지 않으면 규칙3의 기본 우선순위 동작을 그대로 따른다
  17. [추가: 2026-07-23, 수정: 2026-07-24] 설정 화면은 "⚙️ 설정"이라는 메뉴 화면에서 시작하며, "🔎 검색 키워드"·"📰 주요 언론사 선택 및 순서 지정"·"⏰ 스크랩 시간 및 횟수"·"💡 형광펜 단어"·"🖊️ 본문 형식"·"🗑️ 숨긴 기사 관리" 여섯 하위 페이지로 각각 이동해 편집한다 (규칙 6·15·16·5·20·19가 각각 이 하위 페이지에 대응). 메뉴는 수집 관련(키워드·언론사·시간)/표시 관련(형광펜·본문 형식)/관리(숨긴 기사) 세 그룹으로 나눠 보여준다
  18. [추가: 2026-07-23] 화면 폰트는 Pretendard를 우선 사용하고, 없으면 Noto Sans KR, 그래도 없으면 시스템 기본 글꼴 순으로 대체 표시한다
  19. [추가: 2026-07-24] 담당자가 보고에서 제외하고 싶은 기사는 각 기사 옆 🗑️ 버튼으로 화면에서 숨길 수 있다(큐레이션). 숨김은 원본 수집 데이터를 지우지 않고 "숨긴 기사 URL 목록"만 별도로 기록해 화면(메인·지난 기사)과 복사/내보내기 텍스트·워드클라우드에서만 제외하며, 설정 화면의 "🗑️ 숨긴 기사 관리"에서 언제든 "되돌리기"로 다시 표시할 수 있다
  20. [추가: 2026-07-24] 설정 화면 "⏰ 스크랩 시간 및 횟수"에서 자동 실행 시각을 "HH:MM" 단위로 자유롭게 추가·삭제할 수 있다 (최소 1개, 최대 10개 — 너무 잦으면 거의 같은 내용이 새 회차로 계속 쌓여 지난 기사 목록만 어수선해지므로 상한을 둠). 등록한 개수가 곧 하루 실행 횟수가 되며, 화면 상단 "언론 모니터링 [시각] 기준" 제목도 실제로 실행된 시각을 그대로 따라간다(코드 변경 불필요, 기존 구조가 이미 회차 시각을 그대로 표시)
  21. [추가: 2026-07-24, 수정: 2026-07-24] 메인 화면(최신 회차)에서 각 기사 옆 "↑"/"↓" 버튼으로 순서를 수동으로 옮길 수 있다. 같은 소제목 안에서는 순서만 바뀌고, 소제목 맨 위/아래에서 한 번 더 누르면 바로 앞/뒤 소제목으로 기사가 통째로 옮겨간다(소제목 경계 넘나들기). 버튼은 맨 처음 소제목의 첫 기사(↑)·맨 마지막 소제목의 마지막 기사(↓)일 때만 비활성화된다. 이 수동 배정은 그 소제목이 다음 회차 자동 분류에도 존재하는 동안 유지되며, 관련 기사가 모두 사라져 그 소제목 자체가 없어지면 자동으로 원래 분류 방식으로 되돌아간다. 지난 기사 화면은 소제목 재분류를 하지 않아(기능2 규칙 6) 이 기능이 없다. ↑/↓ 버튼은 🗑️ 버튼과 마찬가지로 복사/내보내기 텍스트에는 나타나지 않는다(화면 전용 버튼일 뿐, 별도로 생성되는 순수 텍스트에는 애초에 포함되지 않음)
  22. [추가: 2026-07-24, 수정: 2026-07-24] 메인 화면은 1분마다 자동으로 새로고침된다(`<meta http-equiv="refresh">`, 자바스크립트 불필요, 최초 3분에서 실무자 요청으로 단축). 실시간 스트리밍이 아니라 정해진 스크랩 시각에만 내용이 바뀌는 구조라 이 정도 간격으로 충분하며, 🗑️/↑/↓ 조작은 이미 클릭 즉시 서버에 저장한 뒤 화면을 바꾸는 구조라 새로고침 타이밍이 겹쳐도 잃어버릴 편집 상태가 없다. 숨긴 기사는 화면 생성 시점에 아예 제외되므로 새로고침을 거듭해도 다시 나타나지 않는다. 화면 상단에 "🔄 [시각] 업데이트" 표시기를 두어 마지막으로 화면이 갱신된 시각을 보여주며(선택·복사 불가), 새로고침될 때마다 🔄 아이콘이 1.2초간 CSS 애니메이션으로 회전한다

2) 소제목 자동 분류
- 설명: 기사 요약을 분석해 소제목으로 자동 분류하고, 키워드와 요약을 추출
- AI가 꼭 지켜야 할 규칙:
  1. 소제목은 고정 카테고리가 아니라 매 회차마다 기사 요약(description)을 분석해 자동 생성 (담당자가 추후 검토하므로 완벽한 분류가 아니어도 무방), 최대 5개까지만 생성 — [수정: 2026-07-24] 분류 기준을 기사 제목에서 네이버 요약으로 변경. 제목은 언론사마다 표현이 제각각이라 같은 사건이어도 겹치는 단어가 잘 안 잡히는데, 요약문은 보도자료 내용을 비슷하게 옮기는 경우가 많아 공통 단어가 더 잘 드러남(요약이 비어 있으면 제목으로 대신함)
  2. 별도 AI(LLM) API는 사용하지 않고, 요약에 자주 나오는 단어를 묶는 규칙 기반 방식으로 분류·요약·키워드 추출을 처리 (API 키 비용 없음)
  3. 기사 1건은 소제목 1개에만 배정 (한 소제목에 여러 기사 포함 가능). [추가: 2026-07-24] 단, 담당자가 ↑/↓ 버튼으로 소제목 경계를 넘겨 수동으로 재배정할 수 있으며(규칙21), 이 수동 배정이 자동 분류보다 우선한다
  4. 전체 목록 맨 하단에 "🤖 AI가 추출한 주요 키워드" 항목을 두고, 전체 스크랩 결과를 통틀어 추출한 주요 키워드를 `키워드1, 키워드2, 키워드3` 형식(쉼표 나열)으로 한 번만 표시 [수정: 2026-07-24, 재수정: 2026-07-24 — `#태그` 형식이 지저분하다는 피드백으로 쉼표 나열로 변경]
  5. 전체 목록 맨 하단, 키워드 아래에 "💬 AI가 읽은 소제목별 주요 요약" 항목을 두고, 소제목별로 요약을 각각 작성해 나열 (소제목 이름 + 요약문, 3문장 이내·150자 이내)
  6. [수정: 2026-07-23, 위치 수정: 2026-07-24] 화면에는 최신 회차 결과를 기본으로 표시하되, 화면 상단 버튼 영역(복사·내보내기·설정 옆)의 "📅 지난 기사 더보기" 링크로 최근 7일간 저장된 지난 회차를 날짜별 → 시간대별로 펼쳐볼 수 있음 (7일 보관 정책과 연동되어, 7일이 지나면 조회 목록에서도 사라짐). 최초 결정은 "지난 날짜 조회 기능 없음"이었으나 이후 요청으로 변경됨. 스크롤 없이 바로 접근할 수 있도록 화면 하단에서 상단으로 위치를 옮김(하단에는 대신 "🏠 메인으로 가기"를 배치)
  7. 소제목 안에서 기사는 언론사 우선순위 순(방송사 → 주요 언론사)으로 나열
  8. 첫 번째 소제목을 포함해 모든 소제목 그룹 위에 `<소제목N>` 라벨을 표시 (라벨 없이 나열되는 그룹 없음). [추가: 2026-07-24] 각 소제목 옆 ✏️ 버튼으로 표시 이름을 직접 바꿀 수 있다. 분류 자체(어떤 기사가 어느 그룹인지)는 항상 자동 생성된 원래 단어 기준으로 이뤄지고, 이름표는 화면·복사/내보내기 텍스트에 보여줄 때만 마지막에 적용된다. 같은 단어가 나중에 다른 회차에서 소제목으로 다시 잡히면 이름표가 계속 적용되며, 원래 단어로 다시 바꾸면 이름표가 지워진다

3) 진입 화면 [추가: 2026-07-23]
- 설명: 앱을 실행하면 스크랩 화면 대신 먼저 뜨는 랜딩 화면
- AI가 꼭 지켜야 할 규칙:
  1. 화면 가운데에 로고 이미지를 넣는다 (로고 파일은 추후 제공, 파일이 없으면 자리만 비워두거나 대체 표시)
  2. 로고 아래 "재정경제부 온라인 뉴스 모아보기 🧺👀" 제목 표시 [수정: 2026-07-24]
  3. 워드클라우드 형태로 표시 — 언급량이 많은 키워드는 글자를 크게, 적은 키워드는 작게 (의미 있는 통계가 아니라 직관적 눈요기용). [수정: 2026-07-24] "당일 주요 키워드"라는 설명 문구는 워드클라우드 자체로 직관적으로 알 수 있어 제거. 단어 추출 기준도 기사 제목에서 네이버 요약(description)으로 변경(기능2 규칙 1과 같은 이유). [수정: 2026-07-24] 무궁화를 연상시키는 방사형 3단 배치로 확정 구현 — 배경 그림 없이 단어 배치·색상만 사용(1위: 가운데·굵게·금색, 2~4위: 안쪽 고리·빨강, 5위 이하: 바깥 고리·분홍 계열 순환), 순수 노랑은 밝은 배경에서 안 보여 다크골드로 대체. [알려진 한계] "당일"이라는 표현과 달리, 실제로는 가장 최근 저장된 회차를 그대로 쓴다 — 오늘 첫 스크랩 전에는 어제 마지막 회차의 키워드가 보임
  4. "👓 스크랩 보러 가기" 링크로 실제 스크랩 결과 화면(index.html)으로 이동
  5. "⚙️ 설정" 링크로 설정 메뉴로 이동
  6. 하단에 "문의 📧 sweetgreetings@naver.com" 표시
  7. 앱 실행 시 브라우저는 이 진입 화면을 열며(PRD 4절), 여기서 다시 스크랩 화면으로 이동

**Nice (다음 버전 고려)**
- 이메일 알림 기능: 스크랩된 기사를 담당자 이메일로 자동 발송

---

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

- 범위 (만들 것)
  - 기사 자동 스크랩 (위 5번 기능 1)
  - 소제목 자동 분류 (위 5번 기능 2)
  - 로컬 PC에서 실행 (사용자가 앱을 켜두면 동작하며, 컴퓨터가 잠자기 모드였던 동안 놓친 회차는 깨어난 직후 즉시 보충 실행됨)
- 비범위 (이번엔 만들지 않음)
  - 네이버 뉴스 외 다른 매체(다음, 구글뉴스 등) 연동
  - 사용자별 로그인/계정 관리 기능
  - 이메일 알림 기능 (이번 버전엔 제외, 다음 버전에서 추가 예정)
  - 언론사별 필터/검색 기능 (소제목별 목록만 제공)
  - 같은 네트워크 밖(외부)에서의 모바일 접속 — 우선 윈도우 PC 브라우저에서만 확인, 필요 시 추후 별도 논의

---

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- 공개등급: 내부용 (담당자 개인 업무 도구, 외부 공개 없음)
- 개인정보 여부: 수집 대상은 네이버 뉴스에 공개 게재된 기사로 개인정보 수집 목적은 아님. 다만 기사 본문에 특정인의 실명 등이 포함될 수 있으므로, 결과물을 외부에 공유할 경우 주의가 필요
- API 키 관리: 네이버 검색 API(Client ID / Client Secret)를 사용하며, 발급받은 키는 코드나 저장소에 직접 노출하지 않고 `.env` 파일로 로컬에만 보관
- 데이터 보관: 수집된 기사(추출된 문단 포함)는 7일간 보관 후 자동 삭제
- 저작권: 스크랩 결과는 담당자 개인 업무용으로만 사용하며, 외부 배포·재배포하지 않음
- 실행 환경이 로컬 PC로 한정되어 외부 서버 노출로 인한 보안 위험은 낮음

---

## 8. 기술 스택
*개발에 사용할 도구 모음 (난이도별 옵션 중 내가 고른 것과 그 이유)*

- 선택: 옵션 2 (보통) — 파이썬 웹서버 + 데이터베이스
  - 구성: Python(FastAPI) 웹 서버 + SQLite 데이터베이스(기사 저장) + APScheduler(하루 3회 자동 실행) + 네이버 뉴스 검색 API(기사 검색·요약) + 브라우저에서 `localhost` 주소로 접속해 화면 확인
  - 실행 OS: Windows
- 선택 이유: 코딩을 배운 지 얼마 안 되어 너무 복잡한 구조는 부담스럽고, 옵션 1(파일 저장 방식)만으로는 원하는 파란색 톤·하이라이트 같은 화면 디자인을 구현하기 어려움. "내 컴퓨터에서만 실행" 방침과도 맞고, 색상·하이라이트·소제목별 화면 구성을 자유롭게 만들 수 있어 옵션 2를 선택함

> ※ [수정: 2026-07-23] Design 단계(DESIGN.md)에서 실제로는 "쉬움: 파일 기반" 방식(파이썬 스크립트 + JSON 파일, FastAPI·SQLite·APScheduler 미사용)으로 최종 변경됨. 이유는 구현 난이도를 더 낮추기 위함이며, 화면 디자인 자유도는 정적 HTML로도 유지됨. 자세한 내용은 DESIGN.md 3절 참고.

---

## 9. 개발 단위
*핵심 기능을 만들기 좋은 작은 작업 단위로 나눈 목록 (만들 순서대로 정렬)*

1. 프로젝트 기본 구조 설정 (Python 가상환경, 스크립트 폴더 구성, `.env`로 네이버 API 키 관리) — ※ FastAPI 대신 파일 기반 방식으로 변경 (DESIGN.md 참고)
2. 기사 저장용 로컬 JSON 파일 구조 설계 — ※ SQLite 대신 파일 기반 방식으로 변경 (DESIGN.md 참고)
3. 네이버 뉴스 검색 API 연동, 설정된 키워드 목록(기본값: "재정경제부", "재경부") 중 하나라도 걸리면 기사를 검색·수집하는 기능 구현
4. 수집한 기사를 언론사 우선순위(방송사 → 주요 언론사) 순으로 정렬하는 기능 구현
5. 언론사·기사제목·URL 형식으로 변환하고, API가 제공하는 요약(description)을 DB에 저장하는 기능 구현
6. 저장된 요약에서 일치하는 키워드를 형광 연두색(#C6FF00)으로 하이라이트 처리하는 기능 구현
7. 스크랩 실패 시 5분 간격으로 최대 3회 자동 재시도하는 기능 구현
8. 파이썬 스케줄 루프로 매일 09:00 / 10:30 / 13:30 / 16:30 자동 실행을 예약하는 기능 구현 (잠자기 모드 중에는 실행되지 않으며, 컴퓨터가 깨어난 직후 놓친 회차를 즉시 보충 실행) — ※ APScheduler 대신 경량 스케줄 루프로 변경 (DESIGN.md 참고)
9. 기사 제목을 분석해 소제목(최대 5개)을 자동 생성하고 기사를 배정하는 기능 구현
10. 소제목별 요약(3문장 이내·150자 이내)과 전체 키워드(`#키워드` 형식) 자동 추출 기능 구현 (규칙 기반, 외부 LLM API 없음)
11. 수집된 기사를 7일간 보관 후 자동 삭제하는 기능 구현
12. 소제목별로 기사를 보여주는 웹 화면(파란색 톤, PC 위주 반응형) 구현
13. 수집된 기사가 없을 때 "하나도 없어요" 빈 화면을 표시하는 기능 구현
14. 소제목별 스크랩 목록을 클립보드로 복사하거나 .txt 파일로 내보내는 기능 구현
15. 검색 키워드를 등록·수정·삭제할 수 있는 설정 화면 구현
16. 전체 흐름(스크랩 → 분류 → 화면 표시 → 복사/내보내기) 통합 테스트
